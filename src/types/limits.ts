// Используется только тут. Экспортить лимиты из ctx.limits
export enum Limits {
  // FIXME: debug. Revert it
  priceLevels = 1,
  // FIXME: debug. Revert it
  shifts = 1,
  // FIXME: debug. Revert it
  volumes = 1,
}
